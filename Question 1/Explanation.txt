1. Command: whoami

Explanation:
This command displays the username of the currently logged-in user. The output confirmed that I was logged in as "g2025eb01324".

2. Command: groups

Explanation:
This command displays all the groups that the current user belongs to. The output showed that my user belongs to the groups "g2025eb01324", "adm", "sudo", and "docker".

3. Command: echo $SHELL

Explanation:
This command displays the current shell being used. The output showed "/bin/bash", confirming that the Bash shell is the active command-line interpreter.

4. Command: pwd

Explanation:
This command prints the absolute path of the current working directory. The output confirmed that I was working in the "/home/g2025eb01324/question1" directory.

5. Command: ls -la

Explanation:
This command lists all files and directories, including hidden files, in long format. It displayed the contents of the current directory, including "Environment_Report.txt", file permissions, ownership, size, and timestamps.

6. Command: ping -c 4 google.com

Explanation:
This command sends four ICMP echo requests to Google's server to test internet connectivity. The output showed four successful replies with 0% packet loss, confirming that the system had an active internet connection.
